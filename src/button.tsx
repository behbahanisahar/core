import React, { ReactElement } from "react";

const DKButton = (): ReactElement => {
  return (
    <div>
      <p>my first shared btn component</p>
      <button type="submit">dkg btn</button>
    </div>
  );
};
export default DKButton;
